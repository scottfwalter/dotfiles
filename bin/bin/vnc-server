#!/bin/bash

#ssh -NTC -o ServerAliveInterval=60 -o ExitOnForwardFailure=yes -R 9999:localhost:5900 tunnel@scottwalter.
#ssh -NTC -o ServerAliveInterval=60 -o ExitOnForwardFailure=yes -R 9999:localhost:5900 tunnel@tunnel.scottwalter.net
ssh -NTC -o ServerAliveInterval=60 -o ExitOnForwardFailure=yes -R 9999:localhost:5900 tunnel@scottwalter.net

